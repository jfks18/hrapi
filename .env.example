# Server
PORT=5000
NODE_ENV=production

# CORS
FRONTEND_URL=http://your-frontend-domain
NGROK_URL=
ALLOWED_ORIGINS=capacitor://localhost,ionic://localhost
CORS_ALLOW_ALL=false
# Optional: regex for matching origins, e.g. to allow any ngrok domain
# ALLOWED_ORIGIN_REGEX=.*\.ngrok-free\.app$

# JWT
JWT_SECRET=change_me_to_a_long_random_string

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=your_database

# Optional external user service
USER_SERVICE_URL=

# Email Configuration (for welcome emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=api245481@gmail.com
EMAIL_PASS=qmpcvwpsmkslpiky
EMAIL_FROM="GWC HRMS" <api245481@gmail.com>
